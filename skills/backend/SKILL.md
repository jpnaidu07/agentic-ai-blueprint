---
name: backend
description: Implement API and persistence work packages from an approved solution.
---

# Backend engineering

Read the generated backend engineering packet. Implement typed endpoints, explicit transactions, resource authorization, foreign keys and revision checks. Treat replay and conflicting retries separately. Exercise the acceptance criteria through real storage/API tests; record changed files and test evidence.

Run `agent-blueprint run <solution> backend` for implementation packets where applicable. Specifications remain under `solutions/<solution>/`; do not copy domain decisions into this reusable skill. A packet is an engineering instruction, not proof that code or tests ran. Record completion with `agent-blueprint complete` only after inspecting the implementation and evidence.
